import { View, Text } from "react-native";
import { NativeBaseProvider, Box, Heading, Image, AspectRatio, Center, Stack, HStack } from "native-base";

const Home = () => {
  return (
    <NativeBaseProvider>

  
      
 
  </NativeBaseProvider>
  );
};


export default Home